# Tower Breakers — 클래스 다이어그램

> **작성일**: 2026-03-15
> **대상 버전**: main 브랜치 최신 워킹 카피

---

## 1. 전체 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────┐
│                    GameLifetimeScope                         │
│                 (VContainer LifetimeScope)                   │
│                                                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ CoreDI   │ │ PlayerDI │ │ EnemyDI  │ │ CombatDI     │   │
│  │ Module   │ │ Module   │ │ Module   │ │ Module       │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ TowerDI  │ │ Environ  │ │ UIDI     │ │ RewardDI     │   │
│  │ Module   │ │ DIModule │ │ Module   │ │ Module       │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
│  ┌──────────┐                                               │
│  │ SoundDI  │                                               │
│  │ Module   │                                               │
│  └──────────┘                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 코어 시스템 계층

```
┌─────────────────────────────────────────────────────┐
│                   Core Layer                         │
├─────────────────────────────────────────────────────┤
│                                                     │
│  <<interface>>                                      │
│  ┌──────────────────┐                               │
│  │    IEventBus     │                               │
│  ├──────────────────┤                               │
│  │ +Publish<T>(T)   │                               │
│  │ +Subscribe<T>()  │                               │
│  │ +Unsubscribe<T>()│                               │
│  └────────┬─────────┘                               │
│           │ 구현                                     │
│  ┌────────▼─────────┐                               │
│  │    EventBus      │                               │
│  ├──────────────────┤                               │
│  │ -m_handlers:     │                               │
│  │  Dict<Type,obj>  │                               │
│  └──────────────────┘                               │
│                                                     │
│  ┌──────────────────┐    ┌───────────────────────┐  │
│  │ GameStateMachine │───>│ <<interface>>          │  │
│  ├──────────────────┤    │ IGameState             │  │
│  │ -m_currentState  │    ├───────────────────────┤  │
│  │ +ChangeState<T>()│    │ +OnEnter()            │  │
│  │ +Tick()          │    │ +OnExit()             │  │
│  └──────────────────┘    │ +OnTick()             │  │
│                          └───────┬───────────────┘  │
│            ┌─────────────────────┼──────────┐       │
│            │                     │          │       │
│  ┌─────────▼──┐  ┌──────────────▼┐  ┌──────▼────┐  │
│  │LoadingState│  │ PlayingState  │  │GameOverSt.│  │
│  └────────────┘  ├──────────────┤  └───────────┘  │
│                  │-m_spawner    │                  │
│                  │-m_towerMgr   │                  │
│                  │-m_envMgr     │                  │
│                  └──────────────┘                  │
│                                                     │
│  ┌──────────────────┐                               │
│  │ CooldownSystem   │                               │
│  ├──────────────────┤                               │
│  │ +StartCooldown() │                               │
│  │ +IsReady()       │                               │
│  └──────────────────┘                               │
└─────────────────────────────────────────────────────┘
```

---

## 3. 플레이어 시스템

```
┌───────────────────────────────────────────────────────────────┐
│                      Player Layer                             │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌───────────────────┐         ┌────────────────────────┐     │
│  │   PlayerModel     │         │    PlayerData (SO)     │     │
│  │   (POCO)          │         ├────────────────────────┤     │
│  ├───────────────────┤         │ +BaseAttackPower: int  │     │
│  │ -m_currentLifeCount│         │ +BaseMoveSpeed: float  │     │
│  │ -m_maxLifeCount   │         │ +BaseAttackRange: float│     │
│  │ -m_killCount      │         │ +LeapDistance: float   │     │
│  │ -m_currentWeapon  │         └────────────────────────┘     │
│  │ -m_currentHelmet  │                                        │
│  │ -m_currentBodyArmor│        ┌────────────────────────┐     │
│  ├───────────────────┤         │  WeaponData (SO)       │     │
│  │ +TakeDamage(int)  │         ├────────────────────────┤     │
│  │ +FinalAttackPower()│         │ +AttackPowerBonus: int │     │
│  │ +FinalMoveSpeed() │         │ +AttackRangeBonus: float│    │
│  │ +FinalPushResist  │         │ +AttackSpeedBonus: float│    │
│  │ +EquipWeapon()    │         └────────────────────────┘     │
│  │ +EquipHelmet()    │                                        │
│  │ +EquipBodyArmor() │         ┌────────────────────────┐     │
│  ├───────────────────┤         │  PlayerSkillData (SO)  │     │
│  │ <<event>>         │         ├────────────────────────┤     │
│  │ OnLifeCountChanged│         │ +SkillDamage: int      │     │
│  │ OnWeaponChanged   │         │ +Cooldown: float       │     │
│  │ OnHelmetChanged   │         │ +Range: float          │     │
│  │ OnBodyArmorChanged│         └────────────────────────┘     │
│  └───────────────────┘                                        │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐   │
│  │             PlayerStateMachine                         │   │
│  │                                                        │   │
│  │  ┌──────────┐  ┌──────────┐  ┌───────────┐            │   │
│  │  │PlayerIdle│  │PlayerAtk │  │PlayerLeap │            │   │
│  │  │  State   │  │  State   │  │  State    │            │   │
│  │  └──────────┘  └──────────┘  └───────────┘            │   │
│  │  ┌───────────┐  ┌──────────────┐                      │   │
│  │  │PlayerDef  │  │PlayerSkill   │                      │   │
│  │  │endState   │  │  State       │                      │   │
│  │  └───────────┘  └──────┬───────┘                      │   │
│  └─────────────────────────┼──────────────────────────────┘   │
│                            │ 보유                              │
│             ┌──────────────▼──────────────┐                   │
│             │ <<interface>> ISkillExecutor │                   │
│             ├─────────────────────────────┤                   │
│             │ +IsOnCooldown: bool         │                   │
│             │ +Initialize(...)            │                   │
│             │ +ExecuteAsync(CT): UniTask  │                   │
│             └──────────┬──────────────────┘                   │
│         ┌──────────────┼──────────────┐                       │
│         │              │              │                       │
│  ┌──────▼─────┐ ┌──────▼─────┐ ┌─────▼──────┐               │
│  │Windstorm   │ │Missile     │ │Slash       │               │
│  │SkillExec.  │ │SkillExec.  │ │SkillExec.  │               │
│  └────────────┘ └────────────┘ └────────────┘               │
│                                                               │
│  ┌────────────────────┐    ┌─────────────────────┐           │
│  │    PlayerView      │    │ PlayerPushReceiver   │           │
│  │  (MonoBehaviour)   │    │  (MonoBehaviour)     │           │
│  ├────────────────────┤    ├─────────────────────┤           │
│  │ +PlayAnimation()   │    │ +ApplyPushForce()   │           │
│  │ +SetWeaponSprite() │    │ +SyncPositionDelta()│           │
│  │ +TriggerBackflip() │    │ +IsAtWall: bool     │           │
│  └────────────────────┘    └─────────────────────┘           │
│                                                               │
│  ┌────────────────────┐    ┌─────────────────────┐           │
│  │ PlayerEffectView   │    │ PlayerGuidedMissile │           │
│  │  (MonoBehaviour)   │    │ PlayerSlashProj.    │           │
│  └────────────────────┘    └─────────────────────┘           │
│                                                               │
│  ┌────────────────────┐    ┌─────────────────────┐           │
│  │ AfterimageEffect   │    │ AfterimageSnapshot  │           │
│  └────────────────────┘    └─────────────────────┘           │
└───────────────────────────────────────────────────────────────┘
```

---

## 4. 적 군집 시스템 (핵심)

```
┌──────────────────────────────────────────────────────────────────┐
│                         Enemy Layer                               │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐       │
│  │                   EnemyPushLogic                       │       │
│  │               (군집 알고리즘 핵심)                       │       │
│  │                 (MonoBehaviour)                        │       │
│  ├────────────────────────────────────────────────────────┤       │
│  │ <<static>>                                            │       │
│  │ -s_activeEnemies: List<EnemyPushLogic>                │       │
│  │ -s_generation: int                                    │       │
│  │ +RegisterEnemy(enemy)                                 │       │
│  │ +UnregisterEnemy(enemy)                               │       │
│  │ +InvalidateLeaderCache()                              │       │
│  │ +EnableAllGroupCollidersForSkill()                    │       │
│  │ +DisableAllGroupCollidersForSkill()                   │       │
│  │ +ActiveEnemies: IReadOnlyList                         │       │
│  ├────────────────────────────────────────────────────────┤       │
│  │ <<인스턴스 필드>>                                       │       │
│  │ -m_aheadEnemy: EnemyPushLogic ─────┐  (이중 연결 리스트)│       │
│  │ -m_followerEnemy: EnemyPushLogic ──┘                  │       │
│  │ -m_force: float                                       │       │
│  │ -m_target: PlayerPushReceiver                         │       │
│  │ -m_cachedTransform: Transform                         │       │
│  │ -m_cachedLeader: EnemyPushLogic                       │       │
│  │ -m_cachedLeaderGen: int                               │       │
│  │ -m_cachedBlocked: bool                                │       │
│  │ -m_lastBlockedFrame: int                              │       │
│  │ -m_isSpecialType: bool                                │       │
│  │ +TrainSpacing: float = 1.5f                           │       │
│  ├────────────────────────────────────────────────────────┤       │
│  │ +Initialize(force, target, isSpecial)                 │       │
│  │ +ResetState()                                         │       │
│  │ +SetAheadEnemy(ahead)                                 │       │
│  │ +HandleDeath()                                        │       │
│  │ +GetLeader(): EnemyPushLogic                          │       │
│  │ +IsBlocked(gap): bool                                 │       │
│  │ +IsGroupBlocked(gap): bool                            │       │
│  │ +IsLeaderPushingAtWall(gap): bool                     │       │
│  │ +IsTouchingPlayer(gap): bool                          │       │
│  │ +TryPushPlayer()                                      │       │
│  │ +UpdateCollider()                                     │       │
│  └────────────────────────────────────────────────────────┘       │
│           │                                                       │
│           │ 호출                                                   │
│           ▼                                                       │
│  ┌────────────────────────────────────────────┐                   │
│  │        EnemyMovementHelper (static)        │                   │
│  ├────────────────────────────────────────────┤                   │
│  │ +ExecuteMovement(view, data, pushLogic,   │                   │
│  │   ref isMoving, controller)               │                   │
│  │                                            │                   │
│  │ 내부 흐름:                                   │                   │
│  │  1. IsBlocked(gap) → 차단 판정              │                   │
│  │  2. 애니메이션 전환 (MOVE/IDLE)              │                   │
│  │  3. pos.x -= moveDelta (전진)               │                   │
│  │  4. 간격 클램핑 (TrainSpacing)               │                   │
│  │  5. SyncPositionDelta (플레이어 밀기)        │                   │
│  │  6. TryPushPlayer (힘 전달)                 │                   │
│  └────────────────────────────────────────────┘                   │
│                                                                   │
│  ┌────────────────────────────────────────────┐                   │
│  │           EnemyController                  │                   │
│  │          (MonoBehaviour)                   │                   │
│  │          [코디네이터 패턴]                   │                   │
│  ├────────────────────────────────────────────┤                   │
│  │ -m_stateMachine: EnemyStateMachine         │                   │
│  │ -m_view: EnemyView                         │                   │
│  │ -m_data: EnemyData                         │                   │
│  │ -m_pushLogic: EnemyPushLogic               │                   │
│  │ -m_damageReceiver: EnemyDamageReceiver     │                   │
│  │ -m_debuffSystem: EnemyDebuffSystem         │                   │
│  │ -m_bossFSM: BossFSM                        │                   │
│  │ -m_eventBus: IEventBus                     │                   │
│  ├────────────────────────────────────────────┤                   │
│  │ +Initialize(data, view, pushLogic, ...)    │                   │
│  │ +InitializeAsWaiting(...)                  │                   │
│  │ +TakeDamage(damage, knockback)             │                   │
│  │ +ApplyStun(duration)                       │                   │
│  │ +ApplySlow(multiplier, duration)           │                   │
│  │ +ApplyKnockback(distance, duration, type)  │                   │
│  │ +Heal(amount)                              │                   │
│  │ +Die()                                     │                   │
│  │ +ChangeState<T>()                          │                   │
│  └──────────────┬─────────────────────────────┘                   │
│                 │ 조율                                             │
│    ┌────────────┼────────────┬────────────┐                       │
│    ▼            ▼            ▼            ▼                       │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│ │EnemyStat │ │EnemyDmg  │ │EnemyDe-  │ │BossFSM   │             │
│ │Machine   │ │Receiver  │ │buffSys   │ │(보스전용)  │             │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘             │
│                                                                   │
│  ┌──────────────────────────────────────────────────────┐        │
│  │              EnemyStateMachine                       │        │
│  ├──────────────────────────────────────────────────────┤        │
│  │ -m_states: Dict<Type, IEnemyState>                  │        │
│  │ -m_currentState: IEnemyState                        │        │
│  │ +AddState<T>(state)                                 │        │
│  │ +ChangeState<T>()                                   │        │
│  │ +Tick()                                             │        │
│  │ +IsCurrentState<T>(): bool                          │        │
│  │ +ClearStates()                                      │        │
│  └──────────────────────┬───────────────────────────────┘        │
│                         │ 관리                                    │
│  ┌────────────┬─────────┼──────────┬──────────┬──────────┐       │
│  ▼            ▼         ▼          ▼          ▼          ▼       │
│ ┌─────────┐┌─────────┐┌────────┐┌─────────┐┌────────┐┌───────┐ │
│ │EnemyPush││EnemySup-││EnemyBuf││EnemySho-││EnemySt-││Enemy  │ │
│ │State    ││portPush ││fState  ││otState  ││unnedSt.││Frozen │ │
│ │         ││State    ││        ││         ││        ││State  │ │
│ └─────────┘└─────────┘└────────┘└─────────┘└────────┘└───────┘ │
│ ┌─────────┐                                                     │
│ │EnemyWai-│                                                     │
│ │tingState│                                                     │
│ └─────────┘                                                     │
│                                                                   │
│  <<interface>>                                                    │
│  ┌────────────────────┐                                          │
│  │   IEnemyState      │                                          │
│  ├────────────────────┤                                          │
│  │ +OnEnter()         │                                          │
│  │ +OnExit()          │                                          │
│  │ +OnTick()          │                                          │
│  └────────────────────┘                                          │
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐       │
│  │            EnemySpawner (POCO)                        │       │
│  ├────────────────────────────────────────────────────────┤       │
│  │ -m_factory: EnemyFactory                              │       │
│  │ -m_towerManager: TowerManager                         │       │
│  │ -m_eventBus: IEventBus                                │       │
│  │ -m_spawnMixedPool: List<(EnemyData, float)>           │       │
│  │ -m_activeEnemies: List<EnemyPushLogic>                │       │
│  ├────────────────────────────────────────────────────────┤       │
│  │ +SpawnFloorEnemiesAsync(floorData, ...): UniTask      │       │
│  │ +StopSpawning()                                       │       │
│  │ +GetBossIntroCutscene(floorIndex)                     │       │
│  │ +GetBossController(floorIndex)                        │       │
│  │                                                       │       │
│  │ [스폰 전략]                                            │       │
│  │  패킷 1개 → 순차 스폰                                  │       │
│  │  패킷 2개+ → Fisher-Yates Shuffle → 혼합 스폰          │       │
│  └────────────────────────────────────────────────────────┘       │
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐       │
│  │         EnemyFactory (오브젝트 풀링)                    │       │
│  ├────────────────────────────────────────────────────────┤       │
│  │ -m_pools: Dict<string, IObjectPool<EnemyView>>        │       │
│  │ -m_playerReceiver: PlayerPushReceiver                 │       │
│  │ -m_resolver: IObjectResolver (VContainer)             │       │
│  ├────────────────────────────────────────────────────────┤       │
│  │ +Create(data, position, floorIndex, parent): EnemyView│       │
│  │ +Reclaim(view, name)                                  │       │
│  │ +SetPlayerPushReceiver(receiver)                      │       │
│  └────────────────────────────────────────────────────────┘       │
│                                                                   │
│  ┌─────────────────┐    ┌──────────────────┐                     │
│  │   EnemyView     │    │   EnemyData (SO) │                     │
│  │ (MonoBehaviour) │    ├──────────────────┤                     │
│  ├─────────────────┤    │ +EnemyName       │                     │
│  │ +Initialize()   │    │ +Type: EnemyType │                     │
│  │ +ResetState()   │    │ +MaxHp: int      │                     │
│  │ +PlayAnimation()│    │ +MoveSpeed: float│                     │
│  │ +CachedAnimator │    │ +PushForce: float│                     │
│  └─────────────────┘    │ +EnemyPrefab: GO │                     │
│                         │ +ParryResistance │                     │
│  ┌─────────────────┐    └──────────────────┘                     │
│  │  EnemyDeathEffect│                                            │
│  │ (MonoBehaviour)  │    ┌──────────────────┐                     │
│  └──────────────────┘    │  DeathFragment   │                     │
│                          │ (MonoBehaviour)  │                     │
│                          └──────────────────┘                     │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  <<enum>> EnemyType                                        │  │
│  │  Normal | Tank | SupportBuffer | SupportShooter | Elite | Boss│
│  └─────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 5. 보스 AI 시스템

```
┌──────────────────────────────────────────────────────────────┐
│                      Boss AI Layer                            │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌────────────────────────────────────────────┐               │
│  │             BossFSM                        │               │
│  ├────────────────────────────────────────────┤               │
│  │ -m_controller: EnemyController             │               │
│  │ -m_view: EnemyView                         │               │
│  │ -m_data: EnemyData                         │               │
│  │ -m_phases: List<IBossPhase>                │               │
│  │ -m_currentPhaseIndex: int                  │               │
│  ├────────────────────────────────────────────┤               │
│  │ +Start()                                   │               │
│  │ +Stop()                                    │               │
│  │ +Resume()                                  │               │
│  │ +Stun(duration)                            │               │
│  │ +ChangeState(BossStateType)                │               │
│  │ +CheckPhaseTransition()                    │               │
│  └──────────────┬─────────────────────────────┘               │
│                 │ 보유                                         │
│       ┌─────────┼──────────┬────────────┐                     │
│       ▼         ▼          ▼            ▼                     │
│  ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌──────────┐          │
│  │BossIdle │ │BossAtk  │ │BossStun  │ │BossDead  │          │
│  │State    │ │State    │ │State     │ │State     │          │
│  └─────────┘ └────┬────┘ └──────────┘ └──────────┘          │
│                    │ 패턴 선택                                 │
│       ┌────────────▼─────────────┐                            │
│       │   BossSkillSelector      │                            │
│       │   (행동 트리 기반)         │                            │
│       └────────────┬─────────────┘                            │
│                    │ 실행                                      │
│       ┌────────────▼─────────────┐                            │
│       │  <<interface>>           │                            │
│       │     IBossPattern         │                            │
│       ├──────────────────────────┤                            │
│       │ +PatternName: string     │                            │
│       │ +ExecuteAsync(ctrl, CT)  │                            │
│       └────────────┬─────────────┘                            │
│         ┌──────────┼──────────┬──────────┬──────────┐         │
│         ▼          ▼          ▼          ▼          ▼         │
│  ┌──────────┐┌──────────┐┌──────────┐┌──────────┐┌────────┐  │
│  │GoblinChf ││GoblinChf ││GoblinChf ││GoblinChf ││Goblin  │  │
│  │JumpPat.  ││SwingPat. ││BombPat.  ││Light.Pat.││BuffPat.│  │
│  └──────────┘└──────────┘└──────────┘└──────────┘└────────┘  │
│  ┌──────────┐┌──────────┐┌──────────┐                        │
│  │KrakenArt-││KrakenTen-││KrakenSum-│                        │
│  │illeryPat.││taclePat. ││monTent.  │                        │
│  └──────────┘└──────────┘└──────────┘                        │
│  ┌──────────┐┌──────────┐┌──────────┐                        │
│  │RobotSword││RobotShie-││RobotGun- │                        │
│  │DashPat.  ││ldActive  ││nerAttack │                        │
│  └──────────┘└──────────┘└──────────┘                        │
│                                                               │
│  ┌──────────────────────────────────┐                         │
│  │  <<interface>> IBossPhase        │                         │
│  ├──────────────────────────────────┤                         │
│  │ +PhaseIndex: int                 │                         │
│  │ +HpThreshold: float             │                         │
│  │ +Patterns: List<IBossPattern>   │                         │
│  └──────────┬───────────────────────┘                         │
│      ┌──────┼──────────┐                                      │
│      ▼      ▼          ▼                                      │
│  ┌────────┐┌────────┐┌────────┐                               │
│  │Goblin  ││Kraken  ││Robot   │                               │
│  │Phases  ││Phases  ││Phases  │                               │
│  └────────┘└────────┘└────────┘                               │
│                                                               │
│  ┌────────────────────────────────────────────┐               │
│  │  <<enum>> BossStateType                    │               │
│  │  Idle | Attack | PhaseChange | Stunned | Dead             │
│  └────────────────────────────────────────────┘               │
└──────────────────────────────────────────────────────────────┘
```

---

## 6. 전투 · 이펙트 시스템

```
┌────────────────────────────────────────────────────────┐
│                  Combat & Effects Layer                  │
├────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────┐                       │
│  │   CombatSystem (POCO)       │                       │
│  │   IInitializable, IDisposable│                       │
│  ├──────────────────────────────┤                       │
│  │ -m_eventBus: IEventBus       │                       │
│  │ -m_playerModel: PlayerModel  │                       │
│  │ -m_playerData: PlayerData    │                       │
│  │ -m_towerManager: TowerManager│                       │
│  ├──────────────────────────────┤                       │
│  │ +Initialize()                │                       │
│  │ +Dispose()                   │                       │
│  │ -HandlePlayerPushedAtWall()  │                       │
│  │                              │                       │
│  │ [벽 압착 데미지 흐름]          │                       │
│  │  OnPlayerPushed 구독          │                       │
│  │  → TakeDamage(1)             │                       │
│  │  → OnPlayerDamaged 발행      │                       │
│  │  → OnWallCrushOccurred 발행  │                       │
│  │  → OnHitEffectRequested 발행 │                       │
│  │  → 사망 시 OnGameOver 발행   │                       │
│  └──────────────────────────────┘                       │
│                                                         │
│  ┌──────────────────────────────┐                       │
│  │  CombatEffectPresenter      │                       │
│  │   (MonoBehaviour)           │                       │
│  │  [카메라 쉐이크, 히트스톱 등]  │                       │
│  └──────────────────────────────┘                       │
│                                                         │
│  ┌──────────────────────────────┐                       │
│  │  EffectManager              │                       │
│  │   (MonoBehaviour)           │                       │
│  │  [파티클, VFX 관리]           │                       │
│  └──────────────────────────────┘                       │
│                                                         │
│  ┌──────────────────────────────┐                       │
│  │  EffectDatabase (SO)        │                       │
│  │  [이펙트 ID → 프리팹 매핑]    │                       │
│  └──────────────────────────────┘                       │
└────────────────────────────────────────────────────────┘
```

---

## 7. 타워 · 보상 시스템

```
┌────────────────────────────────────────────────────────┐
│               Tower & Reward Layer                      │
├────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────┐                       │
│  │   TowerManager (POCO)       │                       │
│  ├──────────────────────────────┤                       │
│  │ -m_towerData: TowerData     │                       │
│  │ -m_currentFloorIndex: int   │                       │
│  ├──────────────────────────────┤                       │
│  │ +CurrentFloorIndex: int      │                       │
│  │ +GetFloorData(index)         │                       │
│  │ +RegisterEnemies(floor, type)│                       │
│  │ +OnEnemyKilled(floor)        │                       │
│  │ +IsFloorCleared(floor): bool│                       │
│  └──────────────────────────────┘                       │
│                                                         │
│  ┌──────────────────────────────┐                       │
│  │   TowerData (SO)            │                       │
│  ├──────────────────────────────┤                       │
│  │ +Floors: List<FloorData>    │                       │
│  └──────────┬───────────────────┘                       │
│             │ 포함                                       │
│  ┌──────────▼───────────────────┐                       │
│  │   FloorData                  │                       │
│  ├──────────────────────────────┤                       │
│  │ +EnemyPrefabData: EnemyData │                       │
│  │ +EnemyCount: int            │                       │
│  │ +SpawnInterval: float       │                       │
│  │ +TrainSpacing: float        │                       │
│  │ +SpawnPackets: List<Packet> │                       │
│  │ +IsBossFloor: bool          │                       │
│  └──────────────────────────────┘                       │
│                                                         │
│  ┌──────────────────────────────┐                       │
│  │  RewardChestViewModel       │                       │
│  │  RewardChestView            │                       │
│  │  RewardApplier              │                       │
│  └──────────────────────────────┘                       │
└────────────────────────────────────────────────────────┘
```

---

## 8. UI (MVVM 패턴)

```
┌────────────────────────────────────────────────────────┐
│                     UI Layer                            │
├────────────────────────────────────────────────────────┤
│                                                         │
│  ┌────────────────┐  ←바인딩→  ┌──────────────────┐    │
│  │   HUDView      │           │  HUDViewModel    │    │
│  │ (MonoBehaviour) │           │  (POCO)          │    │
│  └────────────────┘           └──────────────────┘    │
│                                                         │
│  ┌────────────────┐  ←바인딩→  ┌──────────────────┐    │
│  │ EquipmentView  │           │EquipmentViewModel│    │
│  │ (MonoBehaviour) │           │  (POCO)          │    │
│  └────────────────┘           └──────────────────┘    │
│        │ 보유                                           │
│  ┌─────▼──────────┐                                    │
│  │ ItemSlotView   │                                    │
│  │ (MonoBehaviour) │                                    │
│  └────────────────┘                                    │
│                                                         │
│  ┌────────────────┐  ←바인딩→  ┌──────────────────┐    │
│  │ GameOverView   │           │GameOverViewModel │    │
│  │ (MonoBehaviour) │           │  (POCO)          │    │
│  └────────────────┘           └──────────────────┘    │
│                                                         │
│  ┌────────────────┐  ←바인딩→  ┌──────────────────┐    │
│  │ OutGameView    │           │OutGameViewModel  │    │
│  │ (MonoBehaviour) │           │  (POCO)          │    │
│  └────────────────┘           └──────────────────┘    │
│                                                         │
│  ┌────────────────┐                                    │
│  │ DamageTextView │  (오브젝트 풀링)                    │
│  └────────────────┘                                    │
└────────────────────────────────────────────────────────┘
```

---

## 9. 이벤트 통신 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                     EventBus (중앙 메시지 버스)                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [게임 이벤트]                                                    │
│  OnGameStart ──────→ PlayingState, SoundManager                 │
│  OnGameOver ───────→ GameStateMachine, GameOverView              │
│                                                                  │
│  [플레이어 이벤트]                                                │
│  OnPlayerDamaged ──→ HUDViewModel, PlayerEffectView             │
│  OnPlayerPushed ───→ CombatSystem                               │
│  OnPlayerFloorChanged → EnemyController, EnvironmentManager     │
│  OnPlayerAttackLanded → EnemyController (넉백/기절 적용)          │
│  OnPlayerActionStarted → EnemyController (동결 해제)             │
│  OnDefendActionTriggered → EnemyController (패링 넉백)           │
│                                                                  │
│  [적 이벤트]                                                     │
│  OnEnemyDamaged ───→ EnemyController (보스 페이즈 체크)           │
│  OnEnemyKilled ────→ TowerManager, PlayerModel (킬카운트)        │
│  OnEnemyBuffRequested → EnemyController (힐 적용)                │
│                                                                  │
│  [타워 이벤트]                                                    │
│  OnFloorStarted ───→ EnemyController (WAITING→PUSH 전환)        │
│  OnFloorCleared ───→ PlayingState (다음 층 준비)                  │
│  OnFloorReadyForNext → PlayingState                             │
│                                                                  │
│  [연출 이벤트]                                                    │
│  OnHitEffectRequested → CombatEffectPresenter                   │
│  OnDamageTextRequested → DamageTextSpawner                      │
│  OnWallCrushOccurred → EnemyController (전원 동결)               │
│  OnBossIntroEnded ──→ PlayingState                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 10. 군집 알고리즘 데이터 흐름 다이어그램

```
┌─────────────────────────────────────────────────────────────┐
│                  군집 이동 프레임 사이클                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  EnemyController.Update()                                   │
│        │                                                     │
│        ▼                                                     │
│  EnemyStateMachine.Tick()                                   │
│        │                                                     │
│        ▼                                                     │
│  EnemyPushState.OnTick()                                    │
│        │                                                     │
│        ▼                                                     │
│  EnemyMovementHelper.ExecuteMovement()                      │
│        │                                                     │
│        ├─① pushLogic.IsBlocked(gap)                         │
│        │      │                                              │
│        │      ├─ [캐시 히트] m_lastBlockedFrame == frameCount │
│        │      │   └─ return m_cachedBlocked (O(1))           │
│        │      │                                              │
│        │      ├─ IsTouchingPlayer(gap)                      │
│        │      │   └─ target.IsAtWall → blocked              │
│        │      │                                              │
│        │      └─ m_aheadEnemy.IsBlocked(gap) [재귀 전파]     │
│        │          └─ 간격 ≤ TrainSpacing + 0.1 → blocked    │
│        │                                                     │
│        ├─② 애니메이션 전환 (MOVE ↔ IDLE, 상태 변경 시만)      │
│        │                                                     │
│        ├─③ [미차단] pos.x -= moveDelta                       │
│        │      │                                              │
│        │      ├─ 간격 클램핑: pos.x = max(pos.x, ahead.x + spacing)
│        │      │                                              │
│        │      └─ IsTouchingPlayer → SyncPositionDelta       │
│        │                                                     │
│        └─④ TryPushPlayer()                                  │
│               │                                              │
│               └─ 사거리 내 → ApplyPushForce(m_force)        │
│                       │                                      │
│                       ▼                                      │
│              PlayerPushReceiver                              │
│                       │                                      │
│                       ├─ 저항 계수 적용                       │
│                       ├─ 위치 갱신                            │
│                       └─ 벽 도달 → OnPlayerPushed 발행       │
│                               │                              │
│                               ▼                              │
│                        CombatSystem                          │
│                               │                              │
│                               └─ TakeDamage(1) → 사망 판정  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 11. 오브젝트 풀링 생명주기

```
┌─────────────────────────────────────────────────────┐
│              오브젝트 풀 생명주기                      │
├─────────────────────────────────────────────────────┤
│                                                      │
│  EnemyFactory                                       │
│       │                                              │
│       ├── Create(data, pos, floor)                   │
│       │     │                                        │
│       │     ├─ [풀 없음] new ObjectPool<EnemyView>   │
│       │     │   └─ createFunc: Instantiate + Inject  │
│       │     │                                        │
│       │     ├─ pool.Get()                            │
│       │     │   └─ actionOnGet: SetActive(true)      │
│       │     │                                        │
│       │     ├─ view.Initialize() + ResetState()      │
│       │     ├─ pushLogic.Initialize(force, target)   │
│       │     └─ controller.Initialize(data, view, ..) │
│       │                                              │
│       └── Reclaim(view, name)                        │
│             └─ pool.Release(view)                    │
│                 └─ actionOnRelease: SetActive(false)  │
│                                                      │
│  [사망 시 흐름]                                       │
│  EnemyDamageReceiver.Die()                           │
│       ├─ pushLogic.HandleDeath()  (대열 재연결)       │
│       ├─ OnEnemyKilled 이벤트 발행                    │
│       └─ onReclaim(view, name)  → pool.Release()     │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 12. 핵심 의존성 관계 요약

```
GameLifetimeScope
    │
    ├── EventBus ◄──────── 모든 시스템이 구독/발행
    │
    ├── PlayerModel ◄───── CombatSystem, HUDViewModel, EquipmentViewModel
    ├── PlayerStateMachine ◄── PlayerView (Update 루프)
    ├── PlayerView ◄────── PlayingState, ActionButtonSystem
    │
    ├── EnemyFactory ◄──── EnemySpawner
    ├── EnemySpawner ◄──── PlayingState
    │
    ├── CombatSystem ◄──── EventBus (OnPlayerPushed)
    ├── TowerManager ◄──── PlayingState, EnemySpawner, CombatSystem
    │
    ├── EnvironmentManager ◄── PlayingState
    │
    ├── HUDViewModel ◄──── HUDView (바인딩)
    ├── EquipmentViewModel ◄── EquipmentView (바인딩)
    │
    └── SoundManager ◄──── EventBus (게임 이벤트 구독)
```
